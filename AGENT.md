# AGENT

Read `AGENTS.md` first. Use `agent/JANKURAI_STANDARD.md` as the canonical jankurai standard.
For explicit MASTER_PLAN/phase work only, read `agent/MASTER_PLAN.md`, `AUDIT.md`, `phase-logs/index.json`, then the relevant `tips/phases/*.md` file. Log explicit phase work in `phase-logs/`.

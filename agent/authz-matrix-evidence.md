# Authorization Evidence

Canonical evidence lives in `docs/security/authz-matrix.md`.

Current status: Phase 14 owns full ACL and quota proof. This evidence file
exists so audit tools can route security findings to the documented phase
instead of treating the authorization surface as ownerless.

# Contracts Agent Contract

Read the repository root `AGENTS.md` first. Jansu's active protocol contract
sources are the Kafka JSON descriptors in `jansu-sans-io/message/`, generated
Rust surfaces in `jansu-sans-io`, and compatibility claims in
`docs/compatibility/kafka-4.2-ledger.json`.

Do not add a public compatibility claim unless the ledger points to proof from
tests, differential checks, or client checks.

Proof lane: `just compatibility-contract`.

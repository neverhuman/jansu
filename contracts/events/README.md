# Event Contracts

Kafka topic payload schemas live under `etc/schema/` and are exercised through
`jansu-schema` tests. Kafka protocol compatibility is tracked separately in
`docs/compatibility/kafka-4.2-ledger.json`.

# Decisions

Decision records should be added here when an implementation choice affects
compatibility, storage semantics, release posture, or operational risk.

Phase-local tactical notes belong in `tips/phases/` and `phase-logs/`.

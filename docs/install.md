# Install

Developer setup uses the Rust toolchain, `just`, Docker for integration
services, and optional Jankurai tooling for audit evidence.

Common commands:

- `just check`
- `just test`
- `just compatibility-contract`
- `just score`

Use `scripts/ci-doctor.sh` to check local CI prerequisites.

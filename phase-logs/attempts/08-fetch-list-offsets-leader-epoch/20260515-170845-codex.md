# Attempt log

## Agent

Codex

## Prompt

Work on the next logic phase in the master plan.

## Phase Or Audit Item

Phase **08** — `AUDIT-004` (leader-epoch storage contract alignment)

## Files Read

- `AGENTS.md`
- `MASTER_PLAN.md`
- `AUDIT.md`
- `phase-logs/index.json`
- `tips/phases/08-fetch-list-offsets-leader-epoch.md`
- `jansu-storage/tests/log_contract.rs`
- `jansu-storage/tests/offset_for_leader_epoch.rs`
- `jansu-storage/src/lib.rs`

## Files Changed

- `jansu-storage/src/lib.rs`
- `jansu-storage/tests/log_contract.rs`
- `AUDIT.md`
- `phase-logs/08-fetch-list-offsets-leader-epoch.md.log`
- `phase-logs/attempts/08-fetch-list-offsets-leader-epoch/20260515-170845-codex.md`

## Tests Added

- None

## Verification Commands

- `rtk env CARGO_TARGET_DIR=/tmp/jansu-verify-phase08-storage-contract cargo test -p jansu-storage --test log_contract --no-default-features --features dynostore,libsql,slatedb -- --nocapture`
- `rtk env CARGO_TARGET_DIR=/tmp/jansu-verify-phase08-leader-epoch cargo test -p jansu-storage --test offset_for_leader_epoch --no-default-features --features dynostore,libsql,slatedb -- --nocapture`

## Outcome

- Phase 08 storage-contract logging now keeps leader-epoch history explicit in the null backend matrix, while the dedicated leader-epoch behavior proofs stay green.

## Residual Risks

- API 23 advertisement, truncation/recovery, and transactional read_committed proof still remain in Phase 08.

## Next Recommended Action

- Continue Phase 08 on the remaining fetch/list-offsets proof gaps, or move to Phase 09 if the current leader-epoch proof lane is considered complete enough for the active audit.

## Follow-up

- API 23 advertisement promotion completed after the leader-epoch storage proof landed.
- Remaining work in Phase 08 is now the truncation/recovery, transactional `read_committed` / aborted transaction visibility, incremental fetch sessions, topic IDs, broader Fetch max-byte parity, and client matrix proof.

## Timestamp Gap

- A direct attempt to prove timestamp-seek parity against explicit produced timestamps in the in-memory path exposed a gap.
- The gap is now tracked in `docs/VE0X_NATIVE_FEATURE_REQUEST.md` and `FEATURE_GAPS.md`; phase 08 timestamp/seek work remains open.

## Timestamp Contract Tightening

- The in-memory timestamp path now resolves against batch/record timestamps instead of object metadata, and the storage/broker regression suites pass on the non-Postgres feature set.
- `libsql` and `slatedb` still need a backend-specific after-last timestamp fix, so that gap remains documented in the feature trackers.
- Validation now includes deterministic explicit-timestamp coverage for before, middle, and end-of-log cases on the in-memory path.

## Timestamp Gap Closure

- `libsql` now falls back to the latest visible offset when a timestamp seek lands after the end of the log, so the after-last case returns the last visible timestamp and `offset + 1`.
- `slatedb` now uses the final watermark timestamp entry for the after-last timestamp seek case, matching the dynostore contract.
- The storage contract and broker proof both pass with explicit after-last timestamp assertions across dynostore, libsql, and slatedb.

## Verification

- `CARGO_TARGET_DIR=/tmp/jansu-ts-storage cargo test -p jansu-storage --test log_contract --no-default-features --features dynostore,libsql,slatedb -- --nocapture`
- `CARGO_TARGET_DIR=/tmp/jansu-ts-broker cargo test -p jansu-broker --test list_offsets --no-default-features --features dynostore,libsql,slatedb -- --nocapture`

## Result

- The Phase 08 timestamp after-last gap is now closed and documented; the remaining Phase 08 work can proceed on the other open buckets.

# Phase 08 Attempt - Fetch Service Long-Poll Wait

Date: 2026-05-18
Agent: Codex
Branch: feat/storage-fetch-wait

## Scope

Continue the Phase 08 fetch.max.wait.ms work by landing the storage-level `fetch_wait` support and proving it with direct tests. I also checked the broker Fetch request path, but I did not keep that wiring change because it regressed timing in the service path.

## Files Touched

- `jansu-storage/src/batch.rs`
- `jansu-storage/src/dynostore.rs`
- `jansu-storage/src/lib.rs`
- `jansu-storage/src/proxy.rs`
- `jansu-storage/tests/fetch_wait.rs`
- `phase-logs/08-fetch-list-offsets-leader-epoch.md.log`
- `AUDIT.md`

## Ledger Rows Touched

- None.

## Tests Added

- `jansu-storage/tests/fetch_wait.rs::jp_m1::returns_immediately_when_records_exist`
- `jansu-storage/tests/fetch_wait.rs::jp_m1::honours_timeout_when_no_records_arrive`
- `jansu-storage/tests/fetch_wait.rs::jp_m1::wakes_on_concurrent_produce`

## Verification Commands

- `cargo fmt --all --check`
- `git diff --check`
- `env CARGO_TARGET_DIR=/tmp/jansu-verify-fetch-wait cargo test -p jansu-storage --test fetch_wait --no-default-features --features dynostore --jobs 20 -- --nocapture`
- `env CARGO_TARGET_DIR=/tmp/jansu-verify-fetch cargo test -p jansu-storage --test fetch --no-default-features --features dynostore --jobs 20 -- --nocapture`
- `env CARGO_TARGET_DIR=/tmp/jansu-verify-contract cargo test -p jansu-broker --test compatibility_contract --all-features --jobs 20 -- --nocapture`

## Outcomes

- `Storage::fetch_wait` now has a default implementation in the trait, forwarding shims in the storage wrappers, and a notify-driven DynoStore override.
- The direct storage fetch-wait proof is green for immediate return, timeout, and concurrent produce wakeup.
- A broker Fetch request-path experiment was reverted after it regressed timing; the broker service remains on its existing fetch loop for now.

## Residual Risks

- Broker-level long-poll wake behavior is still not claimed.
- The Phase 08 work remaining in `AUDIT-004` is unchanged.
